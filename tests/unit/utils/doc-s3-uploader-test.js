import docS3Uploader from '../../../utils/doc-s3-uploader';
import { module, test } from 'qunit';

module('Unit | Utility | doc s3 uploader');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = docS3Uploader();
  assert.ok(result);
});
